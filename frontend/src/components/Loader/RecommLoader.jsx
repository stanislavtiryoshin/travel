import React from "react";

import styles from "./Loader.module.scss";

const RecommLoader = () => {
  return (
    <div className={styles.hotel_loader}>Loading the recommendations...</div>
  );
};

export default RecommLoader;
